exports.add = (a, b) => a + b
exports.subtract = (a, b) => a - b
exports.multiply = (a, b) => a * b
exports.divide = (a, b) => a / b

// module.exports = {add, subtract, multiply, divide}


// can use either method of exporting but not both in the same file